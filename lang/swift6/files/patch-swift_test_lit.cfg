--- swift/test/lit.cfg.orig	2025-12-09 23:45:54 UTC
+++ swift/test/lit.cfg
@@ -1824,6 +1824,7 @@ elif (run_os in ['linux-gnu', 'linux-gnueabihf', 'free
       config.target_cc_options = ""
     elif run_os == 'freebsd':
       lit_config.note("Testing FreeBSD " + config.variant_triple)
+      config.target_cxx_lib = "-lc++"
       config.target_object_format = "elf"
       config.target_shared_library_prefix = 'lib'
       config.target_shared_library_suffix = ".so"
@@ -1891,6 +1892,8 @@ elif (run_os in ['linux-gnu', 'linux-gnueabihf', 'free
         '%s -target %s %s %s %s %s '
         % (config.swift_frontend, config.variant_triple, config.resource_dir_opt, mcp_opt,
         config.swift_test_options, config.swift_frontend_test_options))
+    if run_os == 'freebsd':
+        config.target_swift_frontend += " -Xcc -stdlib=libc++"
     subst_target_swift_frontend_mock_sdk = config.target_swift_frontend
     subst_target_swift_frontend_mock_sdk_after = ""
     config.target_run = ''
